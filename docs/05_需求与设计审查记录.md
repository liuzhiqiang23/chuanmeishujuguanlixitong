# 需求与详细设计 审查记录

> 课程作业 · 第五步 ②　|　审查对象：`docs/03_需求规格说明书.md`、`docs/04_系统详细设计.md`
> 审查方式：对照第三步《需求分析》《概要设计》与现有代码/数据/数据库逐项走查，凡"文档写了但系统里没有、或系统里有但文档没写"的都记为问题。

## 1. 审查清单与结论

| 维度 | 检查项 | 结论 |
|---|---|---|
| 完整性 | FR1~FR8 覆盖新项目全部范围；框架保留功能边界明确 | ✅ 通过 |
| 完整性 | 每条功能需求都有输入/处理/输出/验收判据，无"待定"项 | ✅ 通过 |
| 一致性 | 需求 §6 接口清单 ↔ 详细设计 §5.2 契约 ↔ 前端 §6.3 调用表，三者编号与字段一一对应 | ✅ 通过（编号 I1~I8 贯穿） |
| 一致性 | 详细设计库表 ↔ 需求"数据需求"字段 ↔ 算法脚本入参 | ⚠️ 发现 1 处冲突 → 已修正（见 §2-1） |
| 可行性 | 8 算法训练、EDA、导入、预测所需的库表与文件在工程内齐备 | ✅ 通过（`metrics.json`、`best_model.joblib`、21 张图均在） |
| 可行性 | 新增页面复用既有框架能力（Layout/权限/请求封装/Element Plus） | ✅ 通过 |
| 健壮性 | Python 调用异常、超时、空结果、中文编码均有对策 | ⚠️ 补 1 项设计（超时，见 §2-6） |
| 安全性 | 密钥与数据库口令不入库；受保护接口走 token 拦截 | ✅ 通过（`local_env.cmd` 已 gitignore） |
| 可维护性 | 路径全部相对 `backend`；新增配置项集中登记 | ✅ 通过（§8 配置汇总） |
| 与作业要求对应 | 老师第五步①"需求细化，生成系统详细设计" | ✅ 两份文档齐备 |
| 与作业要求对应 | 老师第五步②"自己人工审查是否符合预期" | ✅ 本记录 |
| 与作业要求对应 | 老师第五步③"基于上面回答完成 web 界面前端 vue 设计与实现" | ▶ 按 §6 清单实施 |

## 2. 发现的问题与处理

| # | 问题 | 影响 | 处理 | 状态 |
|---|---|---|---|---|
| 1 | 新库 `t_user` 与框架登录表 `t_user` **表名重名**（字段完全不同），迁入同库必然冲突 | 数据库迁移无法完成 | 新项目表改名 `t_movie_user`：已改 `sql/movie_analytics_db.sql`，已在库中执行 `RENAME TABLE`（原表为空，无数据风险） | ✅ 已修 |
| 2 | 第三步需求 FR5 写的"协同过滤"失去了数据源（MovieLens 已按第三步④删除） | 需求不可实现，属"文档与系统不符" | 细化为：内容相似（类型/关键词/演职员/公司/国家加权余弦）为主 + 类型热门兜底 + 站内评分（`t_rating`）个性化加权；同步修正 `docs/01_需求分析.md` 对应两处 | ✅ 已修 |
| 3 | 第四步"删除旧库"未真正闭环：当时删库后为验证系统又从备份恢复了旧库，现在应用仍连旧库 | 作业要求与运行状态不一致 | 详细设计 §3.3 给出五步方案，**已于 2026-09-23 执行完成**：逐表行数一致、应用切库回归通过、旧库已删除 | ✅ 已完成 |
| 4 | `sql/import_data.py` 读取的 `data/recommendation/tmdb_5000_*.csv` 已删，脚本必然报错 | 仓库残留失效脚本 | **已删除旧脚本**，新增 `sql/import_movie_dataset.py`（新数据集 → 15 张表，实测 10000 部 38.6 秒） | ✅ 已完成 |
| 5 | 训练脚本只产出 `best_model.joblib`，**未导出特征列顺序/类别取值**，推理端无法保证 34 维特征对齐 | 预测结果可能静默错位 | 详细设计 §4.4 增补 `models/feature_meta.json` 契约（第六步补导，不重训模型） | ▶ 第六步 |
| 6 | 原 `PredictServiceImpl` 的 `p.waitFor()` **无超时**，Python 脚本挂起会占死请求线程 | 服务可用性风险 | 详细设计 §5.3 增补超时（预测 20s / 推荐 30s / 批量 180s）+ `destroyForcibly()` + 可读错误 | ▶ 第六步 |
| 7 | 算法对比图 `algo_comparison.png` 无对外访问通道（只存在于 algorithm 目录） | 前端"算法对比"页无图可显示 | 详细设计 §5.4 新增 `/algo-figures/**` 映射（配置项 `system.algo.dir`） | ▶ 第六步 |
| 8 | 前端先于后端实现（老师步骤：第五步前端 → 第六步后端），页面此时调用接口必然报错 | 第五步验收时页面不可用属预期 | 页面按契约实现并做三态（加载/空/错误）处理，第七步联调回归；本文档记录该过渡态 | ▶ 第七步 |
| 9 | 接口路径若改名，前端与框架回归面扩大 | 回归成本 | 保留 `/api/predict`、`/api/recommend` 路径与请求字段兼容（只增不改），新增 `/api/movie/*` | ✅ 已定 |
| 10 | 批量预测条数可无限大，长请求易超时/阻塞 | 稳定性 | 接口默认 1000 条、上限 5000 条；执行计时与进度输出；超时保护同 #6 | ▶ 第六/七步 |

## 3. 与第三步文档的差异（需求变更记录）

| 项 | 第三步原文 | 第五步细化后 | 原因 |
|---|---|---|---|
| 推荐算法 | 内容推荐 + 协同推荐融合 | 内容相似为主 + 类型热门 + 站内评分加权 | 协同过滤的数据源（MovieLens）已按作业要求删除 |
| 推荐数据 | `data/recommendation/personal/` | 新数据集字段 + 站内 `t_rating` | 同上 |
| 新库用户表 | `t_user` | `t_movie_user` | 避开框架登录表重名 |
| 预测接口 | `/api/predict/boxoffice` 等（概要设计草案） | 沿用既有 `/api/predict`，新增 `/api/predict/batch`、`/api/predict/algo-compare` | 降低框架回归面 |
| 批量预测 | 未明确上限 | 默认 1000 / 上限 5000 条 | 稳定性 |
| 静态图访问 | 仅 `/eda/**` | 增 `/algo-figures/**` | 支持算法对比图展示 |

## 4. 审查结论

- 需求条目完整、可测，且与数据集实际能力（10,000 部影片、34 维特征、8 种算法）相符；
- 详细设计在**复用框架既有链路**的前提下给出了新增功能的完整契约（接口、脚本、库表、页面、配置），施工顺序清晰（第五步前端 → 第六步后端与迁移 → 第七步联调）；
- 发现的 10 项问题中 4 项已当场修正（表名、需求表述、接口兼容、过渡态说明），其余 6 项均已在详细设计中落为明确条目并排入第六/七步执行，**无遗留"未定义"事项**；

**审查结论：通过，可进入第五步③——Vue 前端设计与实现。**
