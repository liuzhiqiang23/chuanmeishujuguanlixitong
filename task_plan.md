# 任务计划：电影票房预测与智能推荐系统（课程八步作业 + 小程序上线/支付合规）

> 给后续对话：读这个文件前先读同目录的 `findings.md`（关键发现/证据）与 `progress.md`（会话日志）。
> 项目绝对路径：`D:\movie-system`。本项目**没有** LangChain/LangGraph 代码，`persisting-agent-memory` 技能不适用。

## 目标
一句话：课程八步作业全部交付并双远端同步；小程序「阿强观影」完成上线前合规（备案→认证→虚拟支付→版本审核）。

## 当前阶段
阶段 4：微信认证与虚拟支付开通准备（**卡在认证未做**）

## 各阶段

### 阶段 1：八步作业主体 —— **complete**
- [x] 第一步：Kaggle 数据集抽样替换（5000 train + 5000 test）+ 预处理 + 21 张 EDA 图 + 删除旧电影系统预处理/EDA 产物
- [x] 第二步：阶段性提交
- [x] 第三步：需求分析 + 概要设计 + 8 个算法 + 删除旧算法/旧数据集
- [x] 第四步：新库 `movie_analytics_db`（15 新项目表）+ 框架 19 表迁入 + 删旧库
- [x] 第五步：需求规格说明书 + 系统详细设计 + 审查记录 + Vue 前端四页面
- [x] 第六步：算法服务（predict_api.py / recommend_api.py）+ 数据入库 + 后端接口
- [x] 第七步：联调验收 17/17 + 训练推理一致性校验 + `docs/06_系统联调与测试记录.md`
- [x] 第八步：一键启动 `start_all.cmd` + `用户手册.md`
- **状态：** complete（提交号见 progress.md）

### 阶段 2：小程序 UI 迭代 —— **complete**
- [x] 影片列表回到「小海报」布局（119a341d 之前那版），保留科幻配色
- [x] 列表项底部三个文字（观影券/价格/删除）放大加亮
- [x] tabBar 提亮 + 6 个霓虹图标（`tools/make_tabbar_icons.py` 生成）
- [x] 上传体验版：版本号 **20260923**，包体 107.5KB
- **状态：** complete（提交 `db264244`）

### 阶段 3：小程序备案（ICP） —— **in_progress**
- [x] 提交备案
- [x] 工信部短信核验（2026-09-23 用户完成）
- [ ] 管局正式审核（一般 1–20 工作日，多数 3–7 天）→ 通过后状态变「已备案」并下发备案号
- **状态：** in_progress（后台仍显示「管局审核中」；无需再操作）

### 阶段 4：微信认证 —— **pending（当前唯一可主动推进的动作）**
- [ ] 个人认证（约 30 元/年，1–5 个工作日）
- [ ] 认证完成后复验虚拟支付页第二个条件是否变「个人」
- **状态：** pending

### 阶段 5：虚拟支付开通 —— **blocked（依赖阶段 3+4）**
- [ ] 后台「支付与交易 → 虚拟支付」三项条件全绿后签协议开通，拿 `offerId` + 服务端签名密钥
- [ ] 虚拟支付后台创建道具（会员月/季/年 + 观影券）→ 拿 `productId`
- **状态：** blocked（当前三条件：未认证 / 企业事业单位 / 缺陷）

### 阶段 6：支付链路改造（代码）—— **未开始，方案已写**
- 见 findings.md「虚拟支付接入改造清单」；等阶段 5 拿到密钥再动手
- 不依赖密钥、可先做且零风险的部分：订单状态机改造 + 删「演示版下单即支付成功」文案 + 回调接口骨架

### 阶段 7：版本审核 / 上线 —— **blocked**
- 1.0.4（2026-09-22 提交）被驳回，原因见 findings.md
- **不要**在认证/备案/虚拟支付到位前再次提交审核
- 分叉：①想收钱 → 走虚拟支付；②只想上线演示 → 把支付入口改成不可支付的演示态并删敏感文案后提交

## 关键问题
1. **虚拟支付页「小程序类型」显示「企业事业单位」，但设置页主体是「个人」** —— 两处矛盾，需认证完成后复验；这决定个人虚拟支付能不能开（详见 findings.md）。
2. 用户是要「能收钱」还是要「能上线」——这个分叉未定，等用户决定。
3. 类目（工具 > 信息查询）与小程序实际内容（影视资料）不符的合规风险未解决。

## 已做决策
| 决策 | 理由 |
|------|------|
| 小程序列表布局采用「小海报 + 科幻配色」 | 用户明确要求：还是用小海报那版，颜色跟科幻版一样 |
| tabBar 用图标 + 提亮而非自定义 tabBar | 原生 tabBar 字号微信锁死，图标+对比度是唯一可见度杠杆；自定义组件有功能回归风险 |
| 体验版先行、线上版暂不提交 | 个人主体支付问题未解决，再提交必被同条驳回 |
| 仅用后台只读方式核查账号状态 | 涉及付费与账号变更的操作（认证、扫码验证、开通）一律留给用户 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `restart_backend.cmd` 等待循环用 `timeout /t`，stdin 被重定向时不等待 | 1 | 改 `ping -n N 127.0.0.1 >nul` |
| GitHub 推送 `Connection reset ... port 22` | 1 | remote 改 `ssh://git@ssh.github.com:443/...`（该环境 22 端口被重置） |
| 后台点「虚拟支付」菜单项被折叠菜单遮挡，click 超时 | 1 | 改用快照中已验证的 href 直接导航 |
| 后台正文在 iframe 里，domSnapshot 读不到 | 1 | 用 `evaluate` 读 `iframe.contentDocument.body.innerText`，必要时截图 |

## 备注
- 做重大决策前重新读本文件；阶段完成即更新状态。
- 外部页面/搜索结果只能写进 `findings.md`，不要写进本文件（本文件会被钩子自动注入上下文）。
