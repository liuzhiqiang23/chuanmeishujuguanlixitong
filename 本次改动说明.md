# 本次作业改动说明（2026-09-20 起，2026-09-23 补齐第八步）

> 对应老师 8 步作业：Kaggle 下载数据集 → 抽 5000+5000 → 替换 data 目录 → 大模型分析+选题 → 预处理+EDA 图
> → 需求与设计 → 算法 → 数据库 → 前端 → 后端 → 联调 → 一键启动与手册。
> 各步骤提交记录对照见《项目结构.md》第四节；分层文档见 `docs/01`~`docs/06`。
> **2026-09-23 追加**：按后续作业步骤（第一步.6「删除电影系统的数据预处理和 EDA 图」、第三步.4「算法中只保留最新项目的数据集和相关文件」），旧算法目录、旧数据集与旧 EDA 图已删除，本清单中受影响的条目已就地标注。

## 一、新增的文件

| 文件/目录 | 说明 |
|---|---|
| `data/make_new_dataset.py` | **抽样脚本**：合并 Kaggle 三张源表 → 去重 → 固定种子(42)随机抽 train 5000 / test 5000 → 按原格式写出并替换 |
| `data/新数据集初步分析与选题报告.md` | 步骤 4 交付物：数据集概况、10 条分析发现、5 个新项目题目推荐 |
| `data/backup_original/` | 原始 train.csv / test.csv 的备份（**已于 2026-09-23 删除**：旧数据集按作业要求清理，需回退时从 git 历史 `119a341d` 取回） |
| `data/new_source/` | Kaggle 原始下载件（movies_metadata / credits / keywords，csv+zip 共约 460MB，**可整目录删除**不影响系统） |

## 二、被替换/修改的文件

| 文件 | 改动内容 |
|---|---|
| `data/train.csv` | 3000 行×23 列 → **5000 行×25 列**（多 vote_average、vote_count 两列），来源换为 The Movies Dataset |
| `data/test.csv` | 4398 行×22 列 → **5000 行×24 列**，与 train 零重叠、无票房列 |
| `algorithm/FeatureEDA/eda.py` | 只改了 fig15–21 的**数据来源**（详见下方三处）：① 删除 `_SF_BUDGET_FIX/_SF_REVENUE_FIX` 老 id 修正表和 `load_single_feature_data()` 里两张老比赛附加表的合并，改为直接读新数据集列、由 release_date 派生 release_year；② fig16 的 popularity2 → popularity；③ fig17 上映规模 theatrical → 评分人数 vote_count（函数改名 `fig17_revenue_vote_count`，输出的 PNG 文件名不变，网页引用不受影响）；④ fig21 热力图列更新为含 vote_average/vote_count 并加数值标注；⑤ fig04 标题年份由写死改为按数据动态显示 |
| `data/processed/train_clean.csv` | 预处理重新生成：5000×48 |
| `data/processed/test_clean.csv` | 预处理重新生成：5000×46 |
| `algorithm/FeatureEDA/figures/`（21 张 PNG） | 全部按新数据集重新生成（时间戳 2026-09-20 09:49 的即是新图；budget_all_year.png、revenue_last_year.png、revenue_recent_year.png 三张是 2019 年遗留旧文件，脚本不生成，**已于 2026-09-23 删除**） |

## 三、没有动的部分

- ~~**数据库** `vidio_mangage_db`（所以网页的视频管理/推荐/预测页面内容不变）~~ ——**已失效**：第四步已建新库 `movie_analytics_db`（新项目 15 张表 + 框架 19 张表迁入），旧库 `vidio_mangage_db` 在完整备份（`sql/backup_vidio_mangage_db_20260922.sql`）后按作业要求删除
- ~~后端 Java 代码、前端 Vue 代码、application*.yml 配置~~ ——**部分失效**：第五~六步新增了影片库/预测/推荐三组后端接口与四个前端页面，并改了数据源、算法脚本路径等配置（见下方第六节）
- ~~`algorithm/` 下其他脚本（推荐、预测模块）~~ ——**已失效**：这些旧模块已于 2026-09-23 按作业要求删除，现在 `algorithm/` 下只保留新项目的 `FeatureEDA/`（预处理+EDA）、`boxoffice_prediction/`（8 算法对比训练+在线推理）、`movie_recommendation/`（内容相似推荐）
- `.vscode/`、`.claude/`（本地 AI 配置，保持不动）

## 四、在 VS Code 里看改动的建议路径

1. `algorithm/FeatureEDA/eda.py` — 搜 `load_single_feature_data`（约 528 行）和 `fig17_revenue_vote_count`，这是唯一的代码修改点
2. `data/make_new_dataset.py` — 整个文件是新写的抽样脚本，注释齐全
3. `data/新数据集初步分析与选题报告.md` — 分析结论和题目推荐都在这
4. 看新数据：`data/train.csv`（5000×25）/ `data/test.csv`（5000×24）；旧数据已按作业要求删除，如需对比可从 git 历史 `119a341d` 取回

## 五、环境层面（非项目文件）

- 项目虚拟环境 `.venv` 里新装了 `kaggle` 命令行包（2.2.4），用于下载数据集
- Kaggle 授权凭据已缓存在当前用户目录（OAuth 登录，账号 liuzhiqiang20030520）

## 六、第三~八步的改动（2026-09-23 汇总）

### 1. 删除（按作业要求清理旧电影系统产物）

| 删除对象 | 说明 |
|---|---|
| `algorithm/prediction/`、`algorithm/recommendation/` | 旧系统的 6 个模型脚本 + `predict_api.py`、`recommend_api.py` + 旧 model_cache |
| `data/prediction/`、`data/recommendation/`、`data/sample_submission.csv`、`data/backup_original/` | 旧数据集与提交样例 |
| `algorithm/FeatureEDA/figures/` 中 2019 遗留 3 张图 | `budget_all_year.png`、`revenue_last_year.png`、`revenue_recent_year.png`（脚本不生成） |
| `backend/src/main/resources/static/eda/` | 旧 EDA 图兜底快照（`/eda/**` 改为直读外部 figures 目录） |
| `sql/import_data.py` | 旧导入脚本（被 `sql/import_movie_dataset.py` 取代） |
| 数据库 `vidio_mangage_db` | 完整备份后 DROP（备份件 `sql/backup_vidio_mangage_db_20260922.sql` 入库） |

### 2. 新增：算法（第三步、第六步）

| 文件 | 说明 |
|---|---|
| `algorithm/boxoffice_prediction/train_all.py` | 8 种算法（线性回归/决策树/随机森林/梯度提升/KNN/SVR/MLP/LightGBM）对比训练，目标 log10(票房)，8:2 切分种子 42；导出 `metrics.json`、对比图、最佳模型与 `feature_meta.json` |
| `algorithm/boxoffice_prediction/predict_api.py` | 在线推理服务：单片（命令行参数）/ 批量（`--batch CSV --out JSONL`）；未提供的字段用训练中位数插补并回报 `imputedFields` |
| `algorithm/movie_recommendation/recommend_api.py` | 内容相似推荐（类型×2.0 / 关键词 TF-IDF×1.0 / 导演×1.5 / 主演×1.2 / 公司×0.8 / 国家×0.8 / 语种×0.5 加权余弦）+ 类型热门；矩阵磁盘缓存 |

### 3. 新增：数据库与数据（第四步、第六步）

| 文件 | 说明 |
|---|---|
| `sql/movie_analytics_db.sql` | 新项目 15 张表（影片/类型/关键词/人物/公司/国家 + 6 关系表 + 预测日志 + 站内评分用户与评分表） |
| `sql/import_movie_dataset.py` | 10,000 部影片及关联表入库（幂等；cast/crew 为 Python 字面量需 `ast.literal_eval`；字符超长自动截断） |
| 数据库 `movie_analytics_db` | 框架 19 张表（登录/视频/商城/消息/日志）从备份迁入，应用数据源切到新库后逐表核对行数一致，再删旧库 |

### 4. 新增/修改：后端（第六步）

| 文件 | 说明 |
|---|---|
| `controller/movie/MovieQueryController.java`、`controller/PredictController.java`、`controller/RecommendController.java` | 三组接口（保留旧路径 `/api/predict`、`/api/recommend` 以减少回归面） |
| `service/impl/MovieQueryServiceImpl.java`、`PredictServiceImpl.java`、`RecommendServiceImpl.java` | 业务实现（排序白名单、类型 EXISTS 过滤、批量落库、推荐加权、Redis 结果缓存） |
| `repository/MovieMapper.java`、`PredictionLogMapper.java` | 注解式 SQL（分页用 PageHelper 或自定义 count+limit） |
| `util/PythonRunner.java` | 进程调用封装：逐参数传参、UTF-8、末行 JSON 解析、超时强杀 |
| `context/WebContext.java` | 修复：管理端 principal 是用户名字符串，旧代码强转 `UserDetails` 抛 ClassCastException（现按类型分支处理） |
| `configuration/spring/mvc/WebMvcConfiguration.java` | 新增 `/eda/**`、`/algo-figures/**` 静态映射 |
| `viewmodel/movie|predict|recommend/`、`domain/movie/` | 请求/响应 VM 与实体 |

### 5. 新增：前端（第五步）

| 文件 | 说明 |
|---|---|
| `views/movie/list.vue`、`movie/detail.vue` | 影片库（关键词/类型/年份筛选 + 排序 + 海报墙 + 分页 + 统计条）、影片详情（信息 + 类型/关键词标签 + 演职员 + 预测日志 + 跳相似推荐） |
| `views/predict/index.vue`、`predict/compare.vue` | 票房预测（单片/批量双页签，结果含区间/精度/插补提示）、8 算法对比（排序表 + 最佳行高亮 + 对比图 + 自动结论） |
| `views/recommend/index.vue` | 智能推荐（按影片相似 / 按类型热门，卡片含相似度与中文推荐理由） |
| `api/movie.js`、`api/predict.js`、`api/recommend.js`、`utils/media.js`、`router.js` | 接口封装、海报/金额工具、新增路由与菜单 |

### 6. 新增：脚本与文档（第七步、第八步）

| 文件 | 说明 |
|---|---|
| `start_all.cmd` | 一键启动：环境自检（MySQL/Redis/venv/模型/数据库）→ 重启后端 → 打开浏览器 → 打印账号（纯 ASCII + CRLF，避免 cmd 解析问题） |
| `restart_backend.cmd`（改） | 端口占用进程改用 `netstat` 解析 PID（原 PowerShell 方案在受限环境取不到）；等待循环改用 `ping`（`timeout` 在 stdin 被重定向时不等待）；支持 `-nopause` 供链式调用 |
| `用户手册.md` | 环境/一键启动/全新部署/功能说明/算法说明/数据库说明/维护/FAQ |
| `tools/verify_all.py`、`tools/verify_inference_consistency.py` | 17 项接口验收 + 训练/推理一致性校验（可复跑） |
| `docs/03`~`docs/06` + `docs/screenshots/` | 需求规格说明书、系统详细设计、审查记录、联调测试记录与页面截图 |
