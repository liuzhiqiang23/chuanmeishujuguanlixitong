# 《电影票房预测与智能推荐系统》用户手册

> 课程作业 · 第八步②　|　适用版本：2026-09-23（新数据集 + 8 算法票房预测 + 内容相似推荐）
> 配套脚本：`start_all.cmd`（一键启动）、`restart_backend.cmd`（重启后端）、`build_frontend.cmd`（重建前端）、`setup_venv.cmd`（重建 Python 环境）

---

## 1. 系统是什么

以"电影数据分析与推荐"为业务的 Web 系统，由四部分组成：**Vue3 管理后台**（浏览器访问）、**Spring Boot 后端**、**Python 算法引擎**、**MySQL 数据库**；另保留微信小程序端与商城功能。

新项目的核心能力（本次作业新建）：

| 能力 | 说明 | 入口 |
|---|---|---|
| 影片库 | 新数据集 10000 部影片的检索、筛选、详情 | 菜单「影片库 → 影片库（新数据集）」 |
| 票房预测 | 单片表单预测 + 测试集批量预测（含留痕） | 菜单「票房预测 → 票房预测」 |
| 算法对比 | 8 种机器学习/深度学习算法的指标对比 | 菜单「票房预测 → 算法对比」 |
| 智能推荐 | 按影片相似 / 按类型热门，附推荐理由 | 菜单「视频推荐 → 相似影片推荐 / 类型热门推荐」 |
| 特征探索分析 | 21 张 EDA 分析图 | 菜单「数媒数据分析 → 特征探索分析（Python EDA）」 |

技术栈：Spring Boot 3.5（Undertow，端口 8000，包名 `com.alvis.media`）· Vue3 + Vite + Element Plus · MySQL 8（库 `movie_analytics_db`）· Redis/Memurai · Python 3.13（`.venv`）· MyBatis-Plus + PageHelper。

---

## 2. 运行环境

| 软件 | 版本 | 说明 |
|---|---|---|
| JDK | 17+（本机 21） | 后端运行 |
| Maven | 3.9+ | 后端构建 |
| MySQL | 8.x | 库名 `movie_analytics_db`，账号 root / 123456 |
| Redis | 5+（Memurai） | 127.0.0.1:6379，无密码 |
| Python | 3.10~3.13 | 算法引擎（工程自带 `.venv`） |
| Node.js | 18+ | 仅重建前端时需要 |

---

## 3. 一键启动（日常使用）

在工程根目录 `movie-system` 下**双击 `start_all.cmd`**，脚本会依次：

1. 检查 MySQL 3306、Redis 6379、`.venv`、最佳模型文件、数据库 `movie_analytics_db`（缺什么只告警，不自动安装）；
2. 调用 `restart_backend.cmd` 启动后端（自动加载 `local_env.cmd` 里的数据库口令与 RSA 密钥，日志写入 `_boot.log`）；
3. 自动打开浏览器 `http://localhost:8000/admin`；
4. 打印登录账号。

停止服务：关闭那个最小化的 Maven 窗口，或再次双击 `restart_backend.cmd`（它会先杀掉 8000 端口上的旧进程再启动）。

### 登录账号

| 角色 | 用户名 | 密码 | 说明 |
|---|---|---|---|
| 管理员 | 柳志强 | 200306 | 全部功能 |
| 演示账号 | student | 123456 | 一般功能 |

> 账号数据在 `movie_analytics_db.t_user` 表；登录接口传明文口令，服务端用 RSA 私钥解密后校验。

---

## 4. 全新机器部署（从零开始）

### 4.1 准备基础软件
安装 JDK、Maven、MySQL、Redis（Memurai），并保证 MySQL/Redis 已启动。

### 4.2 建库（三步）

```bat
rem ① 新项目 15 张表
mysql -uroot -p123456 < sql\movie_analytics_db.sql

rem ② 框架 19 张表 + 演示数据（用户/影片/商城/日志等）
mysql -uroot -p123456 movie_analytics_db < sql\backup_vidio_mangage_db_20260922.sql

rem ③ 新数据集入库（10000 部影片及其类型/关键词/演职员等关联，约 40 秒）
.venv\Scripts\python.exe sql\import_movie_dataset.py
```

> 数据库口令不是 123456 时，`sql\import_movie_dataset.py` 支持 `--password 你的口令`；
> 后端读取环境变量 `DB_PASSWORD`（`local_env.cmd` 已设置），改口令时同步改 `local_env.cmd`。

### 4.3 Python 环境与模型

```bat
setup_venv.cmd                                  rem 建 .venv 并装依赖（工程自带 .venv 时可跳过）
.venv\Scripts\python.exe algorithm\FeatureEDA\preprocess.py    rem 清洗+特征工程（约 1 分钟）
.venv\Scripts\python.exe algorithm\FeatureEDA\eda.py           rem 生成 21 张 EDA 图
.venv\Scripts\python.exe algorithm\boxoffice_prediction\train_all.py   rem 训练 8 算法并导出最佳模型
```

训练产物：`algorithm/boxoffice_prediction/` 下的 `metrics.json`（指标）、`figures/algo_comparison.png`（对比图）、`models/best_model.joblib` + `models/feature_meta.json`（在线预测用）。

### 4.4 前端（可选，已随工程附带构建产物）

```bat
build_frontend.cmd          rem 需要 Node：构建并把产物同步到 backend/src/main/resources/static/admin
```

### 4.5 启动
双击 `start_all.cmd`。

---

## 5. 功能使用说明

### 5.1 影片库（新数据集）
- 顶部筛选：**关键词**（片名/原始片名）、**类型**（下拉为真实类型分布）、**年份区间**、**排序**（热度/票房/评分人数/年份）；「检索」执行，「重置」清空。
- 结果以海报墙展示：片名、年份、主类型、热度、评分、票房（测试集影片显示"测试集（无票房）"）。
- 点击卡片进入**影片详情**：主信息、类型/国家/公司/关键词标签、导演与主演、**本站预测记录**；右上「看相似影片 →」跳转到推荐页并自动给出结果。
- 海报来自 TMDB 图片 CDN（`image.tmdb.org`）；网络受限时个别海报显示"无海报"，不影响功能。

### 5.2 票房预测（单片）
1. 填写：**制作预算**（必填，美元）、**热度 popularity**、**片长**（必填，分钟）、**原始语言**、**上映月份**（可选）、**主类型**（可选）；亦可点「参考示例」快速填充；
2. 点「开始预测票房」，结果卡片给出：**预测全球票房**（美元 + 亿美元换算）、**参考区间**（10^(ŷ±1.96·RMSE)，对数正态近似）、**对数票房**、**所用模型与验证集指标**；
3. 若某些字段未填写，脚本按**训练集中位数**补齐，界面会黄条提示"哪些字段被补齐"——未提供评分人数等市场信息时，预测不确定性更大，属正常现象。

### 5.3 票房预测（批量）
- 切到「批量预测」页签，设置条数（默认 1000，上限 5000），点「开始批量预测」；
- 对 `data/processed/test_clean.csv` 逐条推理，结果**逐条写入 `t_prediction_log`**；
- 完成后显示：条数、耗时、落库条数、平均预测票房；下方表格为最近预测记录（单片预测也会记录，`movieId` 为空表示表单预测的未入库影片）。

### 5.4 算法对比
- 表格按 **RMSE 升序**显示 8 种算法（RMSE / MAE / R² / 训练耗时），最佳模型整行高亮；
- 下方为算法对比图；结论段落自动引用最佳模型编号与指标。
- 数据来源：`algorithm/boxoffice_prediction/metrics.json`（重跑 `train_all.py` 后刷新页面即更新）。

### 5.5 智能推荐
| 页签 | 操作 | 结果 |
|---|---|---|
| 按影片相似 | 搜索并选择一部参考影片，选推荐数量（5/10/20） | 相似度排序的影片卡片，附**推荐理由**（同类型/同导演/同主演/共同关键词/同语种） |
| 按类型热门 | 选类型（下拉为真实类型分布），选推荐数量 | 该类型内按"0.6·热度 + 0.4·log(评分人数)"排序的影片，附类型/热度/评分人数 |

- 相似度算法：类型×2.0 + 关键词 TF-IDF×1.0 + 导演×1.5 + 前三主演×1.2 + 公司×0.8 + 国家×0.8 + 语种×0.5 的加权余弦；
- **站内评分加权**：若当前登录用户在站内给影片打过 7 分以上（`t_rating` 表），服务端会把这些高分影片的类型偏好作为加权项传给算法（无评分数据时自动跳过，不影响使用）；
- 点击结果卡片可进入影片详情，形成"详情 → 找相似 → 详情"的闭环。

### 5.6 特征探索分析（EDA）
- 21 张图：14 张多维分析（fig01~fig14）+ 7 张单特征/相关性图；点击可放大预览；
- 数据源为 `data/processed/train_clean.csv`（新数据集清洗产物）；重跑 `eda.py` 后刷新页面即见新图（后端 `/eda/**` 直读图片目录，无需重启）。

### 5.7 框架保留功能
登录/用户/角色、媒体管理（影片列表/上传/播放）、消息中心、日志中心、小程序端（会员、优惠券、下单、分类、搜索）均照旧可用，数据在同一库中。

---

## 6. 算法说明

- **任务**：回归，目标 `log10(revenue)`（票房长尾，取对数后近似正态）；
- **数据**：`data/processed/train_clean.csv` 5000 行，按 8:2 切分（随机种子 42）；
- **特征 34 维**：16 个数值/布尔（对数预算、预算缺失指示、时长、演员数、对数热度、log1p(评分人数)、均分、类型数、关键词数、国家数、公司数、语种数、是否系列片、是否英语片、年份、月份）+ 18 个独热（主类型 Top10、主国家 Top6）；
- **8 种算法**：线性回归、决策树、随机森林、梯度提升、KNN、SVR、MLP（深度学习）、LightGBM；需要标准化的模型统一用 `Pipeline(StandardScaler → model)`；
- **当前结果**：随机森林最佳，RMSE 0.875 / MAE 0.5546 / R² 0.591（对数尺度）；
- **在线预测**：只加载 `models/best_model.joblib`（离线训练、在线推理，毫秒级）；未提供的字段按训练集中位数补齐（`models/feature_meta.json` 记录列顺序与类别表，保证训练/推理一致）。

---

## 7. 数据库说明

系统唯一库：**`movie_analytics_db`**（旧库 `vidio_mangage_db` 已按作业要求迁移合并且删除）。

| 分组 | 张数 | 说明 |
|---|---|---|
| 新项目表 | 15 | `t_movie`（10000 部）、`t_genre`/`t_movie_genre`、`t_keyword`/`t_movie_keyword`、`t_person`/`t_movie_cast`/`t_movie_crew`、`t_company`/`t_movie_company`、`t_country`/`t_movie_country`、`t_movie_user`、`t_rating`、`t_prediction_log` |
| 框架表 | 19 | `t_user`/`t_role`/`t_tag`、`t_video_info`/`t_video_tag`/`t_video_play`、消息、日志、商城（优惠券/会员/订单）等 |

常用查询：

```sql
-- 影片总量与有票房数量
SELECT COUNT(*) total, SUM(revenue > 0) with_revenue FROM t_movie;
-- 预测留痕（最近 10 条）
SELECT id, movie_id, model_name, predicted_revenue, created_at
FROM t_prediction_log ORDER BY id DESC LIMIT 10;
-- 类型分布
SELECT g.name, COUNT(*) c FROM t_movie_genre mg JOIN t_genre g ON g.id = mg.genre_id
GROUP BY g.id ORDER BY c DESC;
```

备份与恢复：

```bat
mysqldump -uroot -p123456 movie_analytics_db > backup.sql
mysql -uroot -p123456 movie_analytics_db < backup.sql
```

---

## 8. 日常维护

| 场景 | 操作 |
|---|---|
| 改代码后重启后端 | 双击 `restart_backend.cmd`（或 `start_all.cmd`） |
| 改前端后生效 | 双击 `build_frontend.cmd`（构建+同步+刷新浏览器 Ctrl+F5） |
| 重新训练模型 | `.venv\Scripts\python.exe algorithm\boxoffice_prediction\train_all.py` |
| 只补推理元数据（不重训） | `.venv\Scripts\python.exe algorithm\boxoffice_prediction\train_all.py --dump-meta` |
| 重跑 EDA 图 | `.venv\Scripts\python.exe algorithm\FeatureEDA\eda.py` |
| 重新导入影片数据 | `.venv\Scripts\python.exe sql\import_movie_dataset.py`（幂等） |
| 清推荐缓存 | 删除 `algorithm/movie_recommendation/cache/`（下次请求自动重建） |
| 自检：跑一遍全接口验收 | `.venv\Scripts\python.exe tools\verify_all.py`（17 项，输出验收表） |
| 自检：训练/推理一致性 | `.venv\Scripts\python.exe tools\verify_inference_consistency.py 500` |
| 看后端日志 | 工程根目录 `_boot.log`；运行日志在 `backend/log/` |

> 两项自检的解读与实测数据见 `docs/06_系统联调与测试记录.md` §2、§5。

---

## 9. 常见问题（FAQ）

**Q1：双击 `start_all.cmd` 后浏览器打开是空白/报错？**
后端可能还没起来：等 30~60 秒（Maven 编译+启动），或看 `_boot.log` 尾部有无 `APPLICATION FAILED TO START`。

**Q2：页面能开，但接口报"系统内部错误：Access denied for user 'root'@'localhost'"？**
数据库口令没注入。确认 `local_env.cmd` 存在且 `DB_PASSWORD` 正确（该文件不入库，换机器需重建或直接改 `application-dev.yml`）。

**Q3：登录报错或一直跳回登录页？**
RSA 密钥对缺失。`local_env.cmd` 需包含 `PWD_KEY_PUBLIC_KEY` / `PWD_KEY_PRIVATE_KEY`（可从仓库历史 `git show 31702823:backend/src/main/resources/application.yml` 取回）。

**Q4：预测/推荐报"脚本不存在"或"算法服务超时"？**
确认 `algorithm/boxoffice_prediction/predict_api.py`、`algorithm/movie_recommendation/recommend_api.py` 存在，且 `.venv` 可用；批量预测超时上限 180 秒。

**Q5：预测结果偏高/偏低？**
表单预测只给预算等少量信息，其余字段按训练集中位数补齐；预算与"评分人数/热度"是票房最强信号，未提供时区间会很宽。若想更准，可从影片库选一部真实影片（详情页给出其真实特征）来做参考。

**Q6：海报显示"无海报"？**
TMDB 图片 CDN（`image.tmdb.org`）连接被重置所致，刷新几次即可；功能不受影响。

**Q7：端口 8000 被占用？**
`restart_backend.cmd` 会自动杀掉占用进程；若仍失败，用 `netstat -ano | findstr :8000` 找到 PID 后 `taskkill /PID <pid> /F`。

**Q8：小程序端需要注意什么？**
小程序后端地址在 `miniprogram/app.js` 的 `API_BASES`（依次探测本机、局域网 IP、`https://movie.jszzbinfo.cn`、服务器 IP）；热点重启会换网段，改完需重新预览/上传。

---

## 10. 目录与脚本速查

```
movie-system/
├── start_all.cmd            一键启动（检查环境 → 起后端 → 开浏览器）
├── restart_backend.cmd      重启后端（-nopause 供脚本链式调用）
├── build_frontend.cmd       构建前端并同步到后端静态目录
├── setup_venv.cmd           重建 Python 虚拟环境
├── local_env.cmd            本机密钥/口令（不入库，换机器需重建）
├── algorithm/
│   ├── FeatureEDA/          preprocess.py（清洗+特征）、eda.py（21 张图）、figures/
│   ├── boxoffice_prediction/train_all.py（8 算法训练）、predict_api.py（推理）、metrics.json、models/
│   └── movie_recommendation/recommend_api.py（推荐引擎）、cache/
├── data/
│   ├── train.csv / test.csv  新数据集 5000+5000
│   ├── processed/            清洗与派生特征产物（preprocess.py）
│   ├── new_source/           Kaggle 原始下载件（不入库）
│   └── make_new_dataset.py   数据集抽样脚本
├── sql/
│   ├── movie_analytics_db.sql    新项目 15 张表
│   ├── import_movie_dataset.py   新数据集入库（幂等）
│   └── backup/                   数据库备份（含迁移前快照）
├── backend/                  Spring Boot 后端（8000）
├── frontend/                 Vue3 源码（构建产物在 backend/src/main/resources/static/admin）
├── miniprogram/              微信小程序端
└── docs/                     需求、设计、审查与用户手册
```
