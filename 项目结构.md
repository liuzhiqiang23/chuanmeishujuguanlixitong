# 电影系统项目结构说明

> 更新时间：2026-09-23（第八步完成后刷新）。
> 技术栈：Spring Boot 3.5（Undertow，端口 8000）+ Vue3 管理后台 + MySQL `movie_analytics_db` + Memurai(Redis 6379) + Python 算法引擎（后端以 ProcessBuilder 调用脚本）。
> 各步骤的交付物与提交记录见下方第四节；改动明细见根目录《本次改动说明.md》，使用说明见《用户手册.md》。
> 本文件是**静态文档**，项目变了不会自动更新，需要时说一声即可刷新。

## 一、目录总览（与 VS Code 资源管理器一致）

```
movie-system/
├── algorithm/                        # ★ Python 算法模块（预处理 / EDA / 票房预测 / 智能推荐）
│   ├── FeatureEDA/                   #   数据预处理 + EDA 分析图
│   │   ├── preprocess.py             #     清洗+特征工程：读 data/train.csv → data/processed/*_clean.csv
│   │   ├── eda.py                    #     生成 21 张分析图 → figures/（网页「特征探索分析」引用）
│   │   └── figures/                  #     21 张 PNG（fig01~fig14 多维分析 + 7 张单特征/相关性，均按新数据集重绘）
│   ├── boxoffice_prediction/         #   ★ 票房预测（8 算法对比 + 在线推理）
│   │   ├── train_all.py              #     训练 8 个模型 → metrics.json + figures/algo_comparison.png + models/best_model.joblib
│   │   │                             #     支持 --dump-meta 只导出推理元数据（不重训）
│   │   ├── predict_api.py            #     在线推理服务：单片（命令行参数）/ 批量（--batch CSV → JSONL），stdout 末行输出 JSON
│   │   ├── metrics.json              #     8 算法 RMSE/MAE/R²/耗时（算法对比页数据源）
│   │   ├── models/
│   │   │   ├── best_model.joblib     #     最佳模型（RandomForest，136MB，可由 train_all.py 重训；不入库）
│   │   │   └── feature_meta.json     #     推理元数据（34 列特征、one-hot 类别、中位数插补值；2KB，入库）
│   │   └── figures/algo_comparison.png  # 算法对比图（后端 /algo-figures/** 映射）
│   └── movie_recommendation/         #   ★ 智能推荐（内容相似度 + 类型热门）
│       ├── recommend_api.py          #     两种策略：similar（类型/关键词/演职员/公司/国家加权余弦）、genre_hot
│       └── cache/                    #     相似度矩阵磁盘缓存（首跑生成，可删；不入库）
│
├── backend/                          # ★ Spring Boot 后端（com.alvis.media，Undertow，端口 8000，190 个 Java 文件）
│   ├── src/main/java/.../media/      #   controller / service / repository / domain / configuration 分层
│   │   ├── controller/movie/         #     新增：MovieQueryController（/api/movie/page|detail|stats）
│   │   ├── controller/               #     新增：PredictController（/api/predict、/batch、/logs、/algo-compare）
│   │   │                             #           RecommendController（/api/recommend）
│   │   ├── repository/               #     新增：MovieMapper、PredictionLogMapper（注解式 SQL）
│   │   ├── service/impl/             #     新增：MovieQueryServiceImpl、PredictServiceImpl、RecommendServiceImpl
│   │   ├── util/PythonRunner.java    #     新增：进程调用封装（逐参数传参、UTF-8、末行 JSON、超时强杀）
│   │   ├── viewmodel/movie|predict|recommend/  # 新增：请求/响应 VM
│   │   └── configuration/spring/mvc/WebMvcConfiguration.java  # 新增 /eda/** 与 /algo-figures/** 静态映射
│   ├── src/main/resources/
│   │   ├── application.yml           #   主配置（profile=dev，wx 凭据走环境变量）
│   │   ├── application-dev.yml       #   数据源 movie_analytics_db；system.eda.dir / system.algo.dir /
│   │   │                             #   system.python.script / system.python.predictScript（三个算法脚本路径）
│   │   ├── mapper/                   #   MyBatis 映射 XML（商城 Coupon/Member/Order/CouponShare 四表纯 MyBatis-Plus 无 XML）
│   │   └── static/                   #   运行时静态资源：admin 前端构建产物 / posters 海报 5011 张 / video
│   ├── log/                          #   运行日志 exam.2026MMDD.log
│   ├── target/                       #   Maven 构建产物（后端实际从这里运行，不手改）
│   └── pom.xml
│
├── frontend/                         # ★ Vue3 + Vite 管理后台源码
│   ├── src/views/movie/              #   【新增】list.vue 影片库（筛选+海报墙+分页）、detail.vue 影片详情
│   ├── src/views/predict/            #   index.vue 票房预测（单片/批量双页签）、compare.vue 算法对比
│   ├── src/views/recommend/          #   index.vue 智能推荐（按影片相似/按类型热门）、userList.vue
│   ├── src/views/analysis/           #   edaAnalysis.vue「特征探索分析」引用 /eda/fig01~fig21
│   ├── src/api/movie.js|predict.js   #   【新增】接口封装（page/detail/stats、predict/batch/logs/algoCompare）
│   ├── src/utils/media.js            #   【新增】posterUrl（TMDB w300 前缀）、formatMoney、formatYi
│   ├── src/router.js                 #   路由：/movie/*、/predict/*、/recommend/*、/analysis/eda
│   ├── admin/ + public/              #   历史构建产物副本（build_frontend.cmd 构建后拷入 backend/static/admin）
│   └── node_modules/                 #   前端依赖（生成物，不手改）
│
├── data/                             # ★ 数据集目录（工程唯一数据根）
│   ├── train.csv                     #   新训练集 5000 行×25 列（含 revenue、vote_*）
│   ├── test.csv                      #   新测试集 5000 行×24 列（无 revenue）
│   ├── new_source/                   #   Kaggle The Movies Dataset 原始件（不入库；可整目录删除）
│   ├── make_new_dataset.py           #   抽样脚本：new_source 三表合并 → 种子42抽 5000+5000 → 替换 train/test
│   ├── 新数据集初步分析与选题报告.md #   作业步骤 4 交付物（数据集概况/发现/题推荐）
│   ├── processed/                    #   preprocess.py 产物：train_clean.csv(5000×48) / test_clean.csv(5000×46)
│   ├── tmdb_key.txt                  #   TMDB API key（sync_tmdb.py 用；不入库）
│   ├── sync_tmdb.py                  #   视频模块的 TMDB 影片同步工具（非分析用，保留）
│   └── git_ref_chain.py              #   前端构建产物引用链检查（非分析用，保留）
│                                     #   （8 个海报/中文资料维护脚本已于 2026-09-24 按作业口径清理，需要时可从 git 历史取回）
│
├── miniprogram/                      #   微信小程序端（首页/会员/我的 3 个 tabBar + 分类/搜索/详情/下单确认）
├── shots/                            #   海报维护工作区（二维码、缺失海报清单、填充日志）
│
├── sql/                              # ★ 数据库脚本
│   ├── movie_analytics_db.sql        #   新项目 15 张表（t_movie/t_genre/t_keyword/t_person/t_company/t_country
│   │                                 #   + 6 张关系表 + t_prediction_log + t_movie_user + t_rating）
│   ├── backup_vidio_mangage_db_20260922.sql   # 框架 19 张表 + 演示数据（登录/视频/商城/消息/日志），迁入新库用
│   ├── import_movie_dataset.py       #   新数据集入库脚本（读 processed/*_clean.csv → 15 张表，幂等，约 40 秒）
│   ├── wx_shop.sql / fix_schema.sql / run_fix_schema.cmd   # 商城表、历史修补脚本
│   └── backup/                       #   本机回滚用导出（*.sql 不入库）
│
├── tools/                            # 运维与验收脚本
│   ├── verify_all.py                 #   【第七步】17 项接口联调验收（+ verify_all_result.json）
│   ├── verify_inference_consistency.py  # 【第七步】训练/推理一致性校验（+ verify_consistency_result.json）
│   ├── cloudflared.exe / start_remote.ps1 / tunnel*.cmd|py / tunnel_url.txt   # 内网穿透远程演示
│   └── wx_check.py / wx_contract.py  #   小程序接口自检
│
├── docs/                             # ★ 作业文档（按步骤编号）
│   ├── 01_需求分析.md / 02_概要设计.md            # 第一步：需求分析与概要设计
│   ├── 03_需求规格说明书.md                        # 第五步：需求细化（FR1–FR8 + 验收标准 + 接口清单 I1–I8）
│   ├── 04_系统详细设计.md                          # 第五步：架构/数据库迁移/接口契约/前端页面/配置/风险
│   ├── 05_需求与设计审查记录.md                    # 第五步：12 项自检清单 + 10 个问题及处理
│   ├── 06_系统联调与测试记录.md                    # 第七步：联调链路/验收表/调试记录/性能实测/遗留问题
│   └── screenshots/                                # 第七步：4 个新增页面的浏览器实测截图
│
├── .gitignore                        #   Git 忽略规则（模型/缓存/密钥/大数据集不入库）
├── _boot.log                         #   后端启动日志（restart_backend.cmd 每次覆盖；可删）
├── build_frontend.cmd                #   前端构建并同步到 backend/static/admin
├── restart_backend.cmd               #   重启后端（杀 8000 端口旧进程 → 后台启动 → 等端口 → 健康检查）
├── start_all.cmd                     #   【第八步】一键启动：环境自检 → 重启后端 → 打开浏览器 → 打印账号
├── setup_venv.cmd                    #   初始化 Python 虚拟环境 .venv
├── start_remote.cmd                  #   一键「起后端 + 开穿透隧道 + 改写小程序地址」供远程演示
├── requirements.txt                  #   Python 依赖清单
├── README.md / README.en.md          #   仓库说明（已更新为 movie_analytics_db 三步建库流程）
├── AGENTS.md                         #   【跨会话记忆入口】告诉后续对话先读下面三份进度文件
├── task_plan.md                      #   阶段计划与当前进度（"现在该做什么"）
├── findings.md                       #   事实与证据（后台原文、驳回原因、改造锚点、环境坑）
├── progress.md                       #   会话日志、提交记录、测试结果
├── 用户手册.md                       #   【第八步】用户手册：环境/一键启动/全新部署/功能说明/算法/数据库/FAQ
├── 本次改动说明.md                   #   逐文件改动清单
└── 项目结构.md                       #   本文档
```

> VS Code 资源管理器底部的「大纲 / 时间线 / Java Projects / Maven」是面板功能区，不是项目文件。
> 文件名后绿色 **U** = git 未跟踪（新文件）、橙色 **M** = 已修改；左下角源代码管理图标上的数字 = 未提交变更总数（含约 200 个前端构建产物噪音）。

## 二、数据流（跟着箭头看文件）

### 1. 数据集 → 预处理 → EDA

```
Kaggle The Movies Dataset (45,433 部)
  └─> data/new_source/                     [下载] movies_metadata + credits + keywords
      └─> data/make_new_dataset.py         [合并去重 + 种子42随机抽样]
          ├─> data/train.csv               5000×25（含票房 revenue）
          └─> data/test.csv                5000×24（预测目标，无 revenue）
              └─> algorithm/FeatureEDA/preprocess.py   [清洗+派生特征]
                  ├─> data/processed/train_clean.csv   5000×48
                  └─> data/processed/test_clean.csv    5000×46
                      └─> algorithm/FeatureEDA/eda.py  [绘制]
                          └─> figures/fig01~fig21.png   21 张图
                              └─> 后端 /eda/** 映射（application-dev.yml: system.eda.dir）
                                  └─> 网页「数媒数据分析 → 特征探索分析」
```

### 2. 数据 → 数据库

```
data/processed/*_clean.csv
  └─> sql/import_movie_dataset.py     [幂等导入，长度截断 + Python 字面量解析]
      └─> movie_analytics_db 15 张表   t_movie 10000 / t_movie_cast 83890 / t_movie_keyword 51217 …
          └─> 后端 MovieMapper（注解 SQL）→ /api/movie/page|detail|stats → 网页「影片库」
```

### 3. 离线训练 → 在线推理（票房预测）

```
data/processed/train_clean.csv
  └─> algorithm/boxoffice_prediction/train_all.py   [8 算法 8:2 切分、seed=42、目标 log10(revenue)]
      ├─> metrics.json + figures/algo_comparison.png  →  /api/predict/algo-compare → 网页「算法对比」
      ├─> models/best_model.joblib（RandomForest，RMSE 0.875 / R² 0.591）
      └─> models/feature_meta.json（34 列特征 + one-hot 类别 + 中位数插补值）
          └─> predict_api.py（单片 / 批量）
              └─> PredictServiceImpl（Redis 结果缓存 + 落库 t_prediction_log）
                  └─> /api/predict、/batch、/logs → 网页「票房预测」
```

### 4. 内容相似推荐

```
movie_analytics_db（t_movie + 6 张关系表）
  └─> algorithm/movie_recommendation/recommend_api.py  [加权余弦：类型×2.0 / 关键词×1.0 / 导演×1.5 / 主演×1.2 / 公司×0.8 / 国家×0.8 / 语种×0.5]
      ├─> cache/matrix.npz（矩阵缓存：首次约 14.5s，之后 0.06s 返回）
      └─> strategy=similar | genre_hot（可选 --boost-genres：站内评分类型偏好加权）
          └─> RecommendServiceImpl → /api/recommend → 网页「智能推荐」
```

## 三、关键配置一览（application-dev.yml）

| 配置项 | 值 | 用途 |
| ------ | -- | ---- |
| `spring.datasource.url` | `jdbc:mysql://localhost:3306/movie_analytics_db` | 新库（15 新项目表 + 19 框架表） |
| `system.eda.dir` | `../algorithm/FeatureEDA/figures` | `/eda/**` 图库映射 |
| `system.algo.dir` | `../algorithm/boxoffice_prediction/figures` | `/algo-figures/**` 算法对比图 |
| `system.python.script` | `../algorithm/movie_recommendation/recommend_api.py` | 推荐脚本 |
| `system.python.predictScript` | `../algorithm/boxoffice_prediction/predict_api.py` | 预测脚本 |
| 超时 | 预测 20s / 推荐 30s / 批量 180s | PythonRunner 超时强杀 |

## 四、八步作业与提交记录对照

| 步骤 | 交付物 | 提交 |
| ---- | ------ | ---- |
| 第一步 | Kaggle 数据集抽样替换 + 预处理 + 21 张 EDA 图；删除旧电影系统的预处理/EDA 产物 | `dc056f68` |
| 第二步 | 阶段性提交 | 与第一步同批 |
| 第三步 | 需求分析 + 概要设计（`docs/01`、`docs/02`）+ 8 个算法 + 删除旧算法/旧数据集 | `dc056f68`、`docs/01-02` |
| 第四步 | 新库 `movie_analytics_db` 15 张表；框架表迁入；删除旧库 | `82bbb175`、后续迁库提交 |
| 第五步 | 需求细化（`docs/03`）+ 详细设计（`docs/04`）+ 审查记录（`docs/05`）+ Vue 前端 | `df7c0861`、`cd90e0c3` |
| 第六步 | 算法服务（`0321f3f8`）+ 元数据（`72ea459c`）+ 数据入库与迁库（`940f0e81`）+ 后端接口（`a7665a70`） | 见左 |
| 第七步 | 联调验收 17/17 + 一致性校验 + 测试记录（`docs/06`） | `690b9e4d` |
| 第八步 | 一键启动 `start_all.cmd` + 《用户手册.md》 | 本批提交 |

## 五、大小与忽略项

- 大目录：`backend/target`（构建产物）、`.venv`（Python 环境）、`node_modules`（前端依赖）、`static/posters`（约 480MB 海报）、`algorithm/boxoffice_prediction/models`（136MB 模型）——均为生成物/依赖，不手改
- 不入库（.gitignore）：模型二进制、推荐缓存、`data/new_source/`、`cloudflared.exe`、`tmdb_key.txt`、`local_env.cmd`（密钥）、`sql/backup/*.sql`
  - 例外：`feature_meta.json`（2KB，在线预测必需）与 `sql/backup_vidio_mangage_db_20260922.sql`（建库必需）入库，保证仓库自包含
- `.claude/`、`.vscode/` 为本地 AI/编辑器配置，不属于项目代码，**不要删除**
- `local_env.cmd` 含本机密钥（DB 口令 + RSA 密钥对），**永不入库、永不外发**
