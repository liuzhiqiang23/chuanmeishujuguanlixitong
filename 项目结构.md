# 电影系统项目结构说明

> 更新时间：2026-09-20（换数据集作业完成后，同日晚间补充「发现页 / 分享领券」进行中功能并修正小程序页面数）。技术栈：Spring Boot 3.5 (端口 8000) + Vue3 管理后台 + MySQL `vidio_mangage_db` + Memurai(Redis) + Python 算法引擎（ProcessBuilder 调用）。
> 换数据集作业的改动明细见根目录《本次改动说明.md》。本文件是**静态文档**，项目变了不会自动更新，需要时说一声即可刷新。

## 一、目录总览（与 VS Code 资源管理器一致）

```
movie-system/
├── algorithm/                    # ★ Python 算法模块（EDA / 推荐 / 预测）
│   ├── FeatureEDA/               #   数据预处理 + EDA 分析图（本次作业主战场）
│   │   ├── preprocess.py         #     清洗+特征工程：读 data/train.csv → data/processed/*_clean.csv
│   │   ├── eda.py                #     生成 21 张分析图 → figures/（网页「特征探索分析」直接引用）
│   │   └── figures/              #     21 张 PNG（2026-09-20 全部按新数据集重绘）
│   ├── prediction/               #   票房预测：DecisionTree / KNN / LightGBM / 线性回归 / 随机森林 / SVM + predict_api.py
│   └── recommendation/           #   电影推荐：ensemble / naive / personal 三套推荐器 + recommend_api.py
│
├── backend/                      # ★ Spring Boot 后端（com.alvis.media，Undertow，端口 8000，171 个 Java 文件）
│   ├── src/main/java/.../media/  #   controller 12（根 5 + admin 5 + wx 2）/ service 16+16 / repository 18 / domain 29 / configuration 22 等分层
│   ├── src/main/resources/
│   │   ├── application.yml       #   主配置（profile=dev，wx 凭据走 WX_APPID/WX_SECRET 环境变量）；application-dev.yml 里 system.eda.dir 指向 EDA 图目录
│   │   ├── mapper/               #   MyBatis 映射 XML（11 个；商城 Coupon/Member/Order/CouponShare 四表纯 MyBatis-Plus 无 XML）
│   │   └── static/               #   运行时静态资源：posters 海报 5011 张 / admin 前端构建产物 / eda 图兜底 / video
│   ├── log/                      #   运行日志 exam.2026MMDD.log
│   ├── target/                   #   Maven 构建产物（后端实际从这里运行，不手改）
│   └── pom.xml
│
├── frontend/                     # ★ Vue3 + Vite 管理后台源码
│   ├── src/views/analysis/       #   数据分析页：edaAnalysis.vue「特征探索分析」引用 /eda/fig01~fig21
│   ├── src/views/                #   其余页面：video / recommend / predict / user / dashboard …
│   ├── src/router.js             #   路由：/analysis/eda → edaAnalysis.vue
│   ├── admin/ + public/          #   历史构建产物副本（build_frontend.cmd 构建后拷入 backend/static/admin）
│   └── node_modules/             #   前端依赖（生成物，不手改）
│
├── data/                         # ★ 数据集目录（本次作业全部改动集中在这里，共 21 项）
│   ├── train.csv                 #   【已替换】新训练集 5000 行×25 列（含 revenue、vote_*）
│   ├── test.csv                  #   【已替换】新测试集 5000 行×24 列（无 revenue）
│   ├── backup_original/          #   【新增】原始 train/test 备份（回退用）
│   ├── new_source/               #   【新增】Kaggle The Movies Dataset 原始件（约 460MB，确认不用可删）
│   ├── make_new_dataset.py       #   【新增】抽样脚本：new_source 三表合并 → 种子42抽 5000+5000 → 替换 train/test
│   ├── 新数据集初步分析与选题报告.md # 【新增】作业步骤 4 交付物
│   ├── processed/                #   preprocess.py 产物：train_clean.csv(5000×48) / test_clean.csv(5000×46)
│   ├── prediction/               #   预测模块附加表（老比赛遗留：additionalTrainData、release_dates_per_country 等）
│   ├── recommendation/           #   推荐模块数据：tmdb_5000_movies/credits.csv、personal/（MovieLens 评分）
│   ├── sample_submission.csv     #   老比赛提交模板（遗留，未动）
│   ├── tmdb_key.txt              #   TMDB API key（海报同步脚本用）
│   └── 海报/中文资料维护脚本 ×8   #   backfill_posters / backfill_chinese_names / backfill_chinese_overviews /
│                                 #   download_posters(已弃用) / fetch_current_posters / fill_posters_from_douban /
│                                 #   make_placeholder_posters / verify_and_fill_posters，另有 sync_tmdb、git_ref_chain
│
├── miniprogram/                  #   微信小程序端（7 个页面：首页/会员/我的 3 个 tabBar + 分类/搜索/详情/下单确认 4 个功能页；start_remote.cmd 会改写其 API 地址）
├── shots/                        #   海报维护工作区（preview-qr*.png 二维码、missing_posters.json、填充日志）
├── sql/                          #   建库脚本 vidio_mangage_db.sql、movie_analysis_schema.sql、fix_schema.sql(+run_fix_schema.cmd 一键执行)、wx_shop.sql(商城表)、import_data.py、backup/
├── tools/                        #   内网穿透远程演示（cloudflared.exe、start_remote.ps1、tunnel 脚本与日志）
│
├── .gitignore                    #   Git 忽略规则
├── .cbmignore                    #   AI 代码索引降噪规则（2026-09-19 生成，排除构建产物/依赖/巨型 CSV）
├── _boot.log                     #   历史启动日志（9.9MB，纯遗留垃圾，可删）
├── build_frontend.cmd            #   前端构建并同步到 backend/static/admin
├── restart_backend.cmd           #   重启后端（8000 端口）
├── setup_venv.cmd                #   初始化 Python 虚拟环境 .venv
├── start_remote.cmd              #   一键「起后端 + 开穿透隧道 + 改写小程序地址」供远程演示
├── requirements.txt              #   Python 依赖清单（本次新增依赖：kaggle 2.2.4）
├── README.md / README.en.md      #   Gitee 项目说明（建库时自动生成，内容较旧，未更新）
├── 本次改动说明.md               #   【新增】本次作业逐文件改动清单
└── 项目结构.md                   #   【新增】本文档
```

> VS Code 资源管理器底部的「大纲 / 时间线 / Java Projects / Maven」是面板功能区，不是项目文件。
> 文件名后绿色 **U** = git 未跟踪（新文件）、橙色 **M** = 已修改、左下角源代码管理图标上的数字 = 未提交变更总数（会随操作变动，其中约 200 个是前端构建产物噪音）。

## 二、本次作业的数据流（跟着箭头看文件）

```
Kaggle The Movies Dataset (45,433 部)
  └─> data/new_source/                     [下载] movies_metadata + credits + keywords
      └─> data/make_new_dataset.py         [合并去重 + 种子42随机抽样]
          ├─> data/train.csv               5000×25（含票房 revenue）
          └─> data/test.csv                5000×24（预测目标，无 revenue）
              └─> algorithm/FeatureEDA/preprocess.py   [清洗+27个派生特征]
                  ├─> data/processed/train_clean.csv   5000×48
                  └─> data/processed/test_clean.csv    5000×46
                      └─> algorithm/FeatureEDA/eda.py  [绘制]
                          └─> figures/fig01~fig21.png   21 张图
                              └─> 后端 /eda/** 映射（application-dev.yml: system.eda.dir）
                                  └─> 网页「数媒数据分析 → 特征探索分析」直接展示
```

## 三、进行中的新功能（未提交，2026-09-20 晚）

换数据集作业之外，工作区另有「小程序发现页」与「分享领券」两个功能的源码改动，尚未提交：

### 1. 小程序发现页（分类 / 搜索 / 删片）

| 文件 | 状态 | 职责 |
| --- | --- | --- |
| `controller/wx/WxDiscoverController.java` | 新增 | 分类导航 `/api/wx/category/list`、按标签分页 `/category/videos`、关键词搜索 `/search/videos`、删片 `/admin/video/delete`（controller 内校验管理员 role==3） |
| `service/DiscoverService` + `impl/DiscoverServiceImpl` | 新增 | 分类复用标签体系（t_tag / t_video_tag），selectTopTags 按影片数倒序后用 TMDB 类型中文名白名单（19 种）过滤；删片连带清理标签关联 |
| `VideoTagMapper(.xml)`、`VideoInfoMapper(.xml)` | 修改 | 新增 selectTopTags / countVideosByTag / selectVideosByTag / deleteByVideoId / countByKeyword 等配套 SQL |
| `miniprogram/pages/category/`、`pages/search/` | 新增 | 小程序分类页 + 搜索页（各 4 文件） |

### 2. 分享领券

| 文件 | 状态 | 职责 |
| --- | --- | --- |
| `domain/CouponShare.java`、`repository/CouponShareMapper.java` | 新增 | t_coupon_share 分享记录表（sharerId / receiverId / couponId），靠 uk_sharer_receiver 唯一索引保证同一对用户只发一次，防刷 |
| `controller/wx/WxShopController.java` | 修改 | 新增 `POST /api/wx/coupon/share/receive`（被分享者领券）与 `/share/reward`（分享者奖励）两个接口 |
| `service/CouponService` + `impl/CouponServiceImpl` | 修改 | 分享领券服务方法 |

### 3. 配置变化（application.yml）

- wx 的 appid / secret 改为 `WX_APPID` / `WX_SECRET` 环境变量注入（保留模板默认值兜底）
- 新增 `admin-open-ids` 管理员 openid 白名单（环境变量 `WX_ADMIN_OPEN_IDS`）
- `security-ignore-urls` 追加 `/api/wx/category/**`、`/api/wx/search/**`、`/api/wx/admin/**`

> 另有约 200 个 `backend/src/main/resources/static/admin/static/` 下的哈希命名 js/css 为管理端前端重复构建产物，属构建噪音，无需关注。

## 四、大小与忽略项

- 大目录：`backend/target`（构建产物）、`.venv`（Python 环境）、`node_modules`（前端依赖）、`static/posters`（约 480MB 海报）——均为生成物/依赖，不手改
- `.git/` 仓库此前损坏（objects 大量丢失），已恢复 refs 但历史不可用；VS Code 能显示 U/M 标记（对比的是 intact 的暂存区索引），但 **git diff/log/push 不可用**，改动以《本次改动说明.md》文档记录替代
- `.claude/`、`.vscode/` 为本地 AI/编辑器配置，不属于项目代码
- 可安全删除的遗留物：`_boot.log`（9.9MB 旧日志）、`data/new_source/`（Kaggle 原始件，留着可复现）
